# Local Python environment (not part of the handoff).
.venv/

# Lake build outputs and dependency checkouts (regenerated by
# `lake exe cache get` / `lake build`; several GB).
**/.lake/

# The source book is referenced by SHA-256 only and is not
# redistributed (see Taleb_Lean_Repairs/README.md, "Provenance").
2001.10488v4.pdf

# Scratch extraction used to validate the final archive.
deliverables/_fresh_extract/

# Python bytecode caches.
__pycache__/
